$kind: http-request
url: localhost:5000/clearcall
method: POST
body:
  type: formdata
  content:
    - type: text
      key: callid
      value: "{{callid}}"
    - type: text
      key: deviceid
      value: "{{deviceid}}"
order: 3000
